 - Server exposes API for: 
    - Creating bathroom given latitude and longitude
       - Will create it at that location with a 'pending' verification status
       - ID used will be Postgres's auto generated && auto incrementing `id` column (for now)
       - (TESTING) Will generate a random 64 character [a-zA-Z0-9] string as `temp_data` column
    - Retrieving all bathrooms given a lower left location and an upper right location
       - These will be calculated from the Globe viewport when the zoom level is close enough
       - (TODO) These will be cached in a local DB for minimizing DB hits from user repeatedly zooming in and out 
          - Lookup in local DB before requesting Bathroom data from remote server
             - Maybe achieve by sending all of the Bathroom IDs that are visible in a user's viewport to the server to quickly compare against ones remotely found to know which ones to skip sending back to user ???